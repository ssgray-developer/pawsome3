import 'package:flutter/material.dart';

class VeterinaryClinicScreen extends StatelessWidget {
  const VeterinaryClinicScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
